<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include partials/_headers %>
  </header>

  <p> <%= longURL %> <%= shortURL %></p> 
  <form action="/urls/<%= shortURL %>" method="POST">       
    <input id="longURL" type="text" name="longURL" placeholder="<%= longURL %>" style="width: 250px; height: 20px">
    <input type="submit" value="Update">
  </form>
</body>
</html>